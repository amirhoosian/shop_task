class CardModel {
  String? image;
  String? price;
 CardModel({required this.image, required this.price});
  static List<CardModel> getcrad() {
    List<CardModel> card = [];


  card.add(CardModel(image: ))
  }
}
